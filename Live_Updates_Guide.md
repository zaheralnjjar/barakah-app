# 🔄 دليل إعداد التحديثات المباشرة (Live Updates)

ميزة **Live Updates** تسمح لك بتحديث كود التطبيق (HTML, CSS, JS, Images) وإرساله للمستخدمين فوراً دون الحاجة لرفع نسخة جديدة على متجر التطبيقات أو إعادة بناء ملف APK وتثبيته يدوياً.

> **ملاحظة مهمة:** هذه الخدمة مقدمة من شركة **Ionic** (مطور Capacitor) وهي خدمة سحابية مدفوعة (لها باقة مجانية محدودة).

---

## 🛠️ الخطوة 1: إنشاء حساب Ionic Appflow
1. اذهب إلى موقع [Ionic Appflow](https://ionic.io/appflow).
2. أنشئ حساباً جديداً (Sign Up).
3. بعد الدخول، اضغط على **"New App"**.
4. سمِّ التطبيق (مثلاً: `Barakah App`).
5. اختر **"Capacitor"** كنوع التطبيق.

## 📦 الخطوة 2: تثبيت الإضافة في المشروع
افتح التيرمينال (Terminal) في مجلد مشروعك ونفذ الأمر التالي لربط مشروعك بحساب Ionic وتثبيت إضافة التحديثات:

```bash
npm install -g ionic
ionic init
```

سيطلب منك تسجيل الدخول، ثم اختر "Connect an existing app" واختر التطبيق الذي أنشأته في الخطوة 1.

بعد ذلك، ثبت إضافة التحديثات المباشرة:
```bash
npm install @capacitor/live-updates
npx cap sync
```

## ⚙️ الخطوة 3: إعداد التحديثات (Configuration)
ستحتاج للحصول على `App ID` من لوحة تحكم Ionic Appflow.
افتح ملف `capacitor.config.ts` وأضف إعدادات التحديثات:

```typescript
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.barakah.app',
  appName: 'Barakah',
  webDir: 'dist',
  plugins: {
    LiveUpdates: {
      appId: 'YOUR_IONIC_APP_ID', // <--- ضع المعرف هنا
      channel: 'Production', // القناة الافتراضية
      autoUpdateMethod: 'background', // التحديث في الخلفية
      maxVersions: 2
    }
  }
};

export default config;
```

## 🚀 الخطوة 4: كيفية إرسال تحديث جديد
عندما تقوم بعمل أي تعديل في الكود (مثلاً تغيير لون زر، أو إصلاح نص)، بدلاً من بناء APK جديد، قم بما يلي:

1. **بناء المشروع للويب:**
   ```bash
   npm run build
   ```

2. **رفع التحديث إلى سحابة Ionic:**
   ```bash
   ionic deploy build --channel=Production
   ```

بمجرد اكتمال الرفع، سيقوم التطبيق عند المستخدمين باكتشاف التحديث وتنزيله في الخلفية، وسيتم تطبيقه في المرة القادمة التي يفتحون فيها التطبيق!

---

## ⚠️ متى تحتاج لبناء APK جديد؟
التحديثات المباشرة تعمل فقط مع ملفات الويب (الواجهة والمنطق). يجب عليك بناء APK جديد وتثبيته يدوياً في الحالات التالية:
*   إضافة مكتبة Native جديدة (مثل الكاميرا، البلوتوث).
*   تغيير أيقونة التطبيق أو اسمه.
*   تعديل إعدادات `AndroidManifest.xml` (مثل الأذونات).
*   تغيير إعدادات `capacitor.config.ts` نفسها.
