
# تقرير تحديثات تطبيق "بركة"
**التاريخ:** 23 ديسمبر 2025

## ✅ التحديثات الجديدة المنجزة
تم إجراء التعديلات التالية بناءً على طلبكم:

### 1. 🆕 ميزة الأرشفة وإعادة التعيين (Data Archiving)
- تم إضافة قسم جديد **"أرشفة البيانات وإعادة التعيين"** في صفحة **الإعدادات**.
- **المميزات:**
  - يمكنك اختيار الأقسام التي تريد أرشفتها (المهام، المواعيد، المالية، العادات، التسوق، الملاحظات).
  - عند الأرشفة، يتم حفظ نسخة من البيانات الحالية في الأرشيف للرجوع إليها لاحقاً.
  - يتم **تصفير وحذف** البيانات الحالية من الشاشات لتبدأ صفحة جديدة نظيفة.
  - إمكانية تسمية الأرشيف (مثال: "بيانات 2024").
  - عرض سجل بالأرشيفات السابقة.

> **⚠️ تنبيه هام:** لتفعيل هذه الميزة، يجب إنشاء جدول `archives` في قاعدة البيانات. يرجى استخدام ملف `create_archives_table.sql` المرفق وتنفيذ محتواه في **SQL Editor** في لوحة تحكم Supabase.

### 2. 🕌 نقل أوقات الصلاة للشاشة الرئيسية
- تم نقل عرض **أوقات الصلاة لليوم الحالي** إلى الشاشة الرئيسية (Smart Dashboard).
- الموقع الجديد: **أسفل التقويم الأسبوعي** مباشرة.
- التصميم: بطاقة أنيقة تعرض الأوقات الخمس، مع تمييز الصلاة القادمة والوقت المتبقي لها.

### 3. 🛠️ تحسينات وإصلاحات سابقة (تمت في هذه الجلسة)
- **إصلاح Checbox الخريطة**: حل مشكلة تعارض النقر عند تحديد المواقع المحفوظة.
- **تحسين واجهة الخريطة**: توسيط فوري عند النقر، وزيادة حجم نافذة إضافة الموقع.
- **تحسين القوائم الطويلة**: تحديد قوائم (المواقع، التقرير اليومي، المواعيد) بـ 4 صفوف مع شريط تمرير لتحسين المظهر.
- **طباعة احترافية للتقرير المالي**: استبدال لقطة الشاشة بطباعة مستند HTML منسق وواضح.

---

## 🚀 كيفية التثبيت والتحديث
1. **قاعدة البيانات**: قم بتنفيذ كود SQL المرفق (`create_archives_table.sql`) في Supabase لضمان عمل الأرشيف.
2. **التثبيت**:
   - لبدء التطبيق على المتصفح: `npm run dev`
   - لبناء نسخة Android جديدة: ستحتاج لتشغيل `npx cap sync android` ثم بناء الـ APK كما فعلنا سابقاً.

شكراً لاستخدامك نظام بركة! 🌟
